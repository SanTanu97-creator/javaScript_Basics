/*
// Strings

let str = "  SanTanu Laha   ";

console.log(str[0]);

console.log(str.length);

for(let val of str){
    console.log(val);
}

// Templete Literals is a special type of strings

let str2 = `it is a templete literals ${1+2+3}`;

console.log(str2);

//used to convert lowercase to uppercase
console.log(str.toUpperCase());

//used to convert uppercase to lowercase
console.log(str.toLocaleLowerCase());

//used to remove white spaces before ans after any string
console.log(str.trim());

//used to slice the string
console.log(str.slice(2, 9));

//used to join two strings
console.log(str.concat(str2));

//used to replace the characters
console.log(str.replace("Ta", "tA"));

//used to return character
console.log(str.charAt(5));
*/